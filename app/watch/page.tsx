import { WatchForm } from '@/components/WatchForm';

export const metadata = {
  title: 'Watch a session — Rain Padel',
  description: 'Enter the share code from whoever is running the night and follow the scores live.',
};

export default function WatchPage() {
  return <WatchForm />;
}
